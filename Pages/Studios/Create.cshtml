@page
@model VideoGameApp.Pages.Studios.CreateModel

<div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-2 mb-3">
    <div>
        <h1 class="display-6 mb-0">Create Studio</h1>
        <div class="text-muted small">Add a new game studio to the catalog.</div>
    </div>

    <a class="btn btn-outline-secondary" asp-page="./Index">
        <i class="bi bi-arrow-left me-2"></i>Back
    </a>
</div>

<div class="card shadow-soft">
    <div class="card-body">
        <form method="post" class="row g-3">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="col-md-6">
                <label asp-for="Studio.Name" class="form-label"></label>
                <input asp-for="Studio.Name" class="form-control" />
                <span asp-validation-for="Studio.Name" class="text-danger"></span>
            </div>

            <div class="col-md-6">
                <label asp-for="Studio.Country" class="form-label"></label>
                <input asp-for="Studio.Country" class="form-control" />
                <span asp-validation-for="Studio.Country" class="text-danger"></span>
            </div>

            <div class="col-12 d-flex gap-2 mt-3">
                <button class="btn btn-primary" type="submit">
                    <i class="bi bi-check-lg me-2"></i>Save
                </button>
                <a class="btn btn-outline-secondary"
                   asp-page="./Index"
                   asp-route-returnUrl="@Request.Query["returnUrl"]">
                    Cancel
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}